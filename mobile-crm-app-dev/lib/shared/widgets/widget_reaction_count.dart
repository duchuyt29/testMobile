import 'package:flutter/material.dart';

class WidgetReactionCount extends StatelessWidget {
  const WidgetReactionCount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}
