import 'package:flutter/material.dart';

class PostDetailCommentInfo extends StatelessWidget {
  const PostDetailCommentInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.symmetric(horizontal: 24),
    );
  }
}
